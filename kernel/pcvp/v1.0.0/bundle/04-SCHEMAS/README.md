# Schema Scope

These schemas validate structural invariants only. They do not prove that a claim is true, that evidence is authoritative, that an official Runtime executed, or that an owner decision was valid.

Cross-record and semantic invariants are listed in `handoff.schema.json` under `x-pcvp-cross-record-invariants` and must be enforced by model policy, review, or a future bounded validator.
