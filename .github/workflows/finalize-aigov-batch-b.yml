name: Finalize AIGOV Batch B exact-main

on:
  workflow_run:
    workflows:
      - Validate Main
    types:
      - completed

permissions:
  contents: read
  actions: read
  pull-requests: read

jobs:
  finalize-aigov-batch-b:
    name: Finalize AIGOV Batch B exact-main
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      pull-requests: read
    steps:
      - name: Checkout exact Validate Main head
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Assert exact current-main source identity
        run: |
          set -euo pipefail
          test "${{ github.event.repository.id }}" = "1292378784"
          test "${{ github.event.workflow_run.name }}" = "Validate Main"
          test "${{ github.event.workflow_run.path }}" = ".github/workflows/validate-main.yml"
          test "${{ github.event.workflow_run.event }}" = "push"
          test "${{ github.event.workflow_run.status }}" = "completed"
          test "${{ github.event.workflow_run.conclusion }}" = "success"
          test "$(git rev-parse HEAD)" = "${{ github.event.workflow_run.head_sha }}"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
      - name: Set up Node.js LTS
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies without scripts
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Execute authoritative Batch B final verifier
        env:
          AIGOV_GITHUB_TOKEN: ${{ github.token }}
        run: node tools/verify-aigov-v3-exact-main.mjs --mode batch-b-final --output "${RUNNER_TEMP}/aigov-v4-batch-b-final.json"
      - name: Upload Batch B exact-main closure evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: aigov-v4-batch-b-final-${{ github.event.workflow_run.head_sha }}
          path: ${{ runner.temp }}/aigov-v4-batch-b-final.json
          if-no-files-found: error
          retention-days: 30
